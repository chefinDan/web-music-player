<div class="track-list-container">

      <!-- songs will go here -->
      {{#if artistOnly}}
        {{#each tracks}}
          {{> artistTemplate}}
        {{/each}}
      {{/if}}
      {{#else}}
        {{> trackTemplate}}
